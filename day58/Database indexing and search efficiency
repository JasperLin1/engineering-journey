記憶體 (RAM)：速度極快（比硬碟快十萬倍），但一關機資料就蒸發（易失性）。
硬碟 (Disk / 檔案 / 資料庫)：資料永遠都在，但每次讀取都要讓磁碟轉動或讀寫晶片，速度慢（非易失性）。

問題解決:如果資料庫裡有 1000 萬筆數據，要怎麼在 0.1 秒內找到我們要的那一筆？

優化使用「快取混合模式 (Caching Pattern)」
讀取時載入記憶體，寫入時同步硬碟」的混合策略。

1. 伺服器剛開機時（只讀取一次硬碟）：
在伺服器剛啟動的瞬間，去讀取 JSON 檔案，並把它存在一個記憶體變數裡（叫它 cached_data）。

[開機] ──> 讀取硬碟 JSON ──> 存入記憶體變數 cached_data

2. 當有 1 萬個用戶戳 API 時（讀取記憶體）：
所有的請求，都直接去讀取記憶體裡的 cached_data。因為是記憶體，速度極快，而且完全不會去碰硬碟。硬碟在此時是處於安靜休息的狀態。

用戶 A、B、C... ──> 讀取記憶體 cached_data (0.0001秒完成)

3. 當管理員新增或修改對照表時（寫入記憶體 + 寫入硬碟）：
如果真的有資料修改，才同時做兩件事：
更新記憶體裡的 cached_data，確保當前服務拿得到最新資料。
把新資料寫入硬碟 JSON 檔案，確保安全存檔。

4. 當伺服器突然關機、重啟時：
記憶體裡的 cached_data 確實瞬間消失了。但是沒關係！因為在步驟 3 有存檔在硬碟。 當伺服器重新開機時，它會回到步驟 1，再次從硬碟讀入最新資料到記憶體中。