CAP 定理 (Consistency, Availability, Partition Tolerance)
指出在⼀個分散式系統中，當節點間無法通訊的狀況發⽣時，不可能同時完全滿⾜以下三個特性
    1. ⼀致性（Consistency）：所有節點在同⼀時間看到的資料是⼀致的。
    2. 可⽤性（Availability）：每個請求都能在有限時間內獲得回應，不會長時間等待。
    3. 分區容錯性（Partition Tolerance）：系統能在網路分區或訊息丟失的情況下繼續運作。

    根據 CAP 定理，⼀個系統最多只能同時滿⾜其中的兩個特性。例如：
    CP 系統：保證⼀致性與分區容錯，但在網路分區時可能犧牲可⽤性。
    AP 系統：保證可⽤性與分區容錯，但可能犧牲⼀致性。
    CA 系統：保證⼀致性與可⽤性，但在真實世界中幾乎不可能實現，因為網路分區
            不可避免。



❌ 為什麼不能「三取三」？
要達成 CAP 三取三，唯一的可能就是 「網路永遠不會斷、永遠零延遲」。
但在現實世界

光速有限：封包傳輸需要時間。

硬體會壞：挖土機可能會挖斷光纖，路由器可能會當機。

雲端特性：你用的 Render 在美國東部，MongoDB 可能在另一個機房，
         它們之間的通訊只要卡住 0.1 秒，系統就得面臨「要等（保證一致）」還是「不等（直接回應）」的抉擇。