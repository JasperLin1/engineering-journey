2dsphere 是 MongoDB 資料庫引擎內建的技術。
底層核心技術主要源自於 Google 開源的 S2 Geometry 函數庫 (S2 Geometry Library)。
1.立方體投影： 將地球（球體）投影到一個正六面體（立方體）的六個面上。
2.希爾伯特曲線 (Hilbert Curve)： 在這六個面上，使用一種能填滿空間的連續曲線（希爾伯特曲線）將二維的網格空間轉換成「一維的數值」（稱為 S2 Cell ID）。
3.階層式網格： 地球表面被細分成 30 個不同層級的網格，最大可以覆蓋整個大洲，最小可以精細到幾平方公分。

主要用於地理空間收尋，網格就是 2dsphere 這種 index 實現目錄的方法。
其他還有:注音/拼音目錄（找單字）→ 對照資料庫的 一般索引（B-Tree Index），用來找 Email 或名字。
        時序年表（找歷史事件）→ 對照資料庫的 時間索引（Date Index），用來按時間排序活動。
